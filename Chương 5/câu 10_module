def bubbleSort(nlist):
    n = len(nlist)
    for i in range(n - 1):  
        for j in range(n - i - 1): 
            if nlist[j] > nlist[j + 1]:
                nlist[j], nlist[j + 1] = nlist[j + 1], nlist[j]
